name: Build iOS ESP
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Linux Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang build-essential git perl libssl-dev

      - name: Setup Theos environment variables
        run: |
          echo "THEOS=/home/runner/theos" >> $GITHUB_ENV

      - name: Install Theos Framework
        run: |
          git clone --recursive https://github.com /home/runner/theos
          curl -LO https://github.com
          tar -xf iPhoneOS14.5.sdk.tar.xz -C /home/runner/theos/sdks/
          rm iPhoneOS14.5.sdk.tar.xz

      - name: Compile Tweak
        run: |
          export THEOS=/home/runner/theos
          make ARCHS="arm64" TARGET="iphone:clang:14.5:14.0" FINALPACKAGE=1
          
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: MyCleanESP
          path: .theos/obj/MyCleanESP.dylib
